[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip3 install --break-system-packages -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 ingest.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
